export addNums(num1, num2) {
    if(num1 === undefined || null || num2 === undefined || null) return false
    //if(num1 === undefined || num2 === undefined) return false
    
    return num1 + num2;
}